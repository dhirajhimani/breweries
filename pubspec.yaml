name: breweries
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  dio: ^5.4.3+1
  flutter_bloc: ^8.1.5
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.13
  flutter_lints: ^2.0.0
  bloc_test: ^9.1.7
  mockito: ^5.4.4

flutter:
  uses-material-design: true
